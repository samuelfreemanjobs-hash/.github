# Revenue Intel Agent — v5.0 — Quick start

1. Paste `REVENUE-INTEL-AGENT-v5.0-SYSTEM-PROMPT.txt` → **system** role.  
2. Set `today`, `niche`, `icp` in `RUNTIME-CONTEXT.example.yaml` → inject `<runtime_context>`.  
3. Default output: **ExecutiveBriefMD** only (add `JSON` to `output_modes` if you need machine-readable).  
4. Run: *Niche/Market: … / ICP: …*  
5. Read **What I rejected and why** — often the highest-value section.  
6. Hypothesis rows → complete `validation_plan` before selling.

**Product:** Revenue Intel Agent — v5.0 · Freeman Intelligence · `{SUPPORT_EMAIL}`
